# System configuration (auto-detect)
device: auto  # auto, cpu, cuda, mps

# Distributed training
num_processes: 1
use_fsdp: false
use_deepspeed: false

# Performance
dataloader_num_workers: 4
pin_memory: true
